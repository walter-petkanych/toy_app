<p id="notice"><%= notice %></p>

<h1>Listing Microposts</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th>Votes +</th>
      <th>Votes -</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @microposts.each do |micropost| %>
      <tr>
        <td><%= micropost.content %></td>
        <td><%= micropost.user_id %></td>
        <%
=begin%>
 path автоматично приймає url заданий в файлі роутс 
<%
=end%>
        <td> <a href="<%= new_path(micropost.id) %>"><%= micropost.votes.where(vote:true).count %></a></td>
        <td> <a href="<%= new_path(micropost.id) %>"><%= micropost.votes.where(vote:false).count
 %></a></td>
        <td><%= link_to 'Show', micropost %></td>
        <td><%= link_to 'Edit', edit_micropost_path(micropost) %></td>
        <td><%= link_to 'Destroy', micropost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Micropost', new_micropost_path %>
